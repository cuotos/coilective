node_modules/
.netlify/
.DS_Store

# generated by netlify dev's edge-functions setup; we use none
deno.lock

# the shared password, for local dev only — never committed
.env

# written by the build; says when the deploy was made
public/version.json
