{
  "name": "coilective",
  "private": true,
  "type": "module",
  "description": "Group filament orders: a shared wishlist and who owes what",
  "scripts": {
    "dev": "netlify dev",
    "test": "node --test \"netlify/lib/*.test.mjs\"",
    "catalogue": "node scripts/catalogue.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^27.5.2"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
